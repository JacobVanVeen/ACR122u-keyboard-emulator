# ACR122u-keyboard-emulator
A python script for the ACR122u reader that reads nfc tags and types the first text record it finds through a keyboard emulator.

## Instalation:
Run pip -r requirements.txt

Then simply run reader,py. To make it run in the background (windowsless) rename it to reader.pyw. 
